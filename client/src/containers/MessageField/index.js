import React from "react";
import { useStyles } from "./style";

const MessageField = () => {
  const classes = useStyles();

  return <div className={classes.root}>MainContentField</div>;
};

export default MessageField;
