import React from "react";
import { useStyles } from "./style";

const Sidebar = () => {
  const classes = useStyles();

  return <section className={classes.root}>SIDEBAR</section>;
};

export default Sidebar;
